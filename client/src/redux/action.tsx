/* actions are basically, anything that can happen in our application, 
when a user clicks on a button, when data finishes loading, when data starts loading, 
any action that could potentially change the state of our entire application is considered an action */

export const loginButtonClicked = {
  type: "LOGIN_BUTTON_CLICKED",
};
